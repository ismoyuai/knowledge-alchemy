---
type: agents-context
status: active
created: 2026-09-16
version: 1.0
tags:
  - system
  - agents
  - para
---

# AGENTS.md — 墨宇Logic的知识炼金炉 · 协作上下文

> 面向所有 AI 助手/Agent。进这个库之前先读这一页。
>
> **系统本质**：这是一条「动态的流水线」，不是一座「静态的图书馆」。

---

## 一、核心驱动引擎：三环迭代（The Engine）

这是知识库的生命力来源，解决**信息如何转化为认知**。

```
第一环：入口触发            第二环：验证加工            第三环：认知演进
(Input/Trigger)      →     (Processing/Filter)   →    (Output/Evolution)
─────────────────          ────────────────────        ──────────────────
灵感库入盒                  Skills 流水线               内容输出
(灵感 / RSS / Flomo)       (验证 / 加工 / 铸卡)         (发布 / 迭代)
[入盒规则]                 [门禁四问]                  [认知迭代线]
                                  ↑
                        （反馈闭环：认知升级）
```

| 环 | 干什么 | 规则定义在哪 |
|---|---|---|
| 第一环 · 入口触发 | 灵感 / RSS / Flomo 触发 → 灵感库入盒 | `00-灵感库/index.md` 的入盒规则 |
| 第二环 · 验证加工 | Skills 流水线跑「验证 → 加工 → 铸卡」 | `00-灵感库/index.md` 的门禁四问 + `00-灵感库/创作流程.md` |
| 第三环 · 认知演进 | 内容输出（发布 / 迭代），并回灌第一环 | 本文件第四节「四条铁律」 |

**反馈闭环**：第三环的产出必须回灌到第一环——上一轮的内容里长出的新问题，就是下一轮的灵感。断了这个环，系统就退回成图书馆。

**三环落在哪**：

| 环 | 产物落到哪 |
|---|---|
| 第一环 · 入口触发 | `00-灵感库/`（还没锻造过的原始灵感） |
| 第二环 · 验证加工 | `07-原子库/`（铸成原子卡片） |
| 第三环 · 认知演进 | `06-Wiki/`（成篇体系）或 `01-Projects/`（项目产出） |

回灌第一环的原料 = 上一次成篇时冒出来的新问题。

### 第二环的分流闸门（现在用 / 未来用）

加工完一条判断后，**先别问"这对不对"，先问"什么时候能用"**：

```
   ┌─ 现在能用 → 立刻列 1-2-3 行动项 → 照做（进 01-Projects/ 或直接执行，不落盘）
   │
   └─ 未来能用 → 铸成原子卡 → 07-原子库/
                   ⚠️ 必须写「触发条件」：什么情况下该取出来用
```

**没有触发条件的「未来能用」= 收藏。** 触发条件写进卡片的绿灯（适用场景），这是「知识卡片」Skill 四铁律已经要求的东西。

---

## 二、物理存储结构：PARA 目录（The Container）

知识库的骨架，解决**信息如何各司其职**。

```
墨宇Logic的知识炼金炉/
├── AGENTS.md                 ← 【规则】这个库怎么用（Agent 必读）
├── User.md                   ← 【档案】面对的人是谁（给建议前先读）
├── 00-灵感库/                ← 【核心战场】新想法、创作素材（Dynamic）
│   ├── index.md             入盒规则 + 门禁四问
│   └── 创作流程.md           灵感 → 灵感引擎 → 挖矿 → 输出
├── 01-Projects/              ← 【任务态】有明确截止日期的具体项目
├── 02-Areas/                 ← 【责任态】长期关注、需要持续维护的领域（e.g. 知识周转系统）
├── 03-Resources/             ← 【资料库】参考资料、Canvas、工具集
├── 04-Archive/               ← 【归档区】已完成或不再活跃的内容
├── 05-Skills/                ← 【技能层】您的思维模型、SOP、Prompt 模板
├── 06-Wiki/                  ← 【知识层】成篇的、成体系的知识条目
├── 07-原子库/                ← 【原料层】原子卡片：一卡一个概念（图外新增）
├── Attachments/              ← 附件 / 图片
├── Books/                    ← 书籍笔记
├── Clippings/                ← 网页剪藏
├── Templates/                ← 笔记模板
└── vault-scripts/            ← 自动化工具集（rss_tool.py、search.py 等）
```

### 目录契约（输入 → 输出）

| 目录 | 什么进来 | 什么出去 | 验证方式 |
|---|---|---|---|
| `00-灵感库/` | 让你不舒服的问题（不认同的 / 讲不清的 / 困惑的 / 旧认知被动摇的） | 过门禁四问 → 铸卡 → 内容输出；没过 → 删掉 | **≤5 篇，无超 2 周未处理** |
| `01-Projects/` | 有目标 + 有截止日期 | 完成 → `04-Archive/` | 每个项目有 `status` + `due_date` |
| `02-Areas/` | 长期责任，无截止日期 | 持续产出 SOP / 方法论 | 每季度回顾一次方向 |
| `03-Resources/` | 外部输入：参考、Canvas、工具 | 被灵感库 / 卡片 / 输出引用 | 每月至少 5 篇被链出 |
| `04-Archive/` | 完成的项目、不再活跃的内容 | 不再读取 | 季度清理一次 |
| `05-Skills/` | 思维模型、SOP、Prompt | 被任何 agent 调用 | **不动**——这是最值钱的资产 |
| `06-Wiki/` | 由原子卡编织成的体系化长文、方法论 | 被引用 / 修正 / 重写；过时 → `04-Archive/` | 脱离写作上下文也能独立读懂 |
| `07-原子库/` | 过了门禁四问、被挖矿压缩过的单点判断 | 被组合成 `06-Wiki/`；被 `01` / `02` 引用 | 每张卡有红灯，且能被别的卡引用 |
| `Attachments/` `Books/` `Clippings/` `Templates/` `vault-scripts/` | 附件 / 书摘 / 剪藏 / 模板 / 脚本 | 被上面 8 个目录引用 | — |

**规则**：

- 不在上述 **13 个目录**之外新建目录。
- **洞见卡（原子卡片）住在 `07-原子库/`**；`00-灵感库/` 只放**还没锻造过**的原始灵感，不放已经铸成的卡。
- **不允许出现空目录**：每个目录必须有一个 `README.md`，写清本目录的【标签】+ 什么进来 + 什么出去。规则全文在本文件，README 只讲本目录自己的职责。

---

## 三、Skills（4 个，在 `05-Skills/`）

**这是本库最重要的资产**——工具会换，提示词不会。随时可以迁移到任何 agent（WorkBuddy / Claude Code / Codex / Pi Agent）。

| Skill | 触发词 | 干什么 | 通过标准 |
|---|---|---|---|
| `灵感/` | 灵感、我有一个灵感、帮我发散一下 | 7 刃切割，把模糊想法锻成锋利洞见 | 至少一刃让你觉得"这个角度我没想过" |
| `挖矿/` | 挖矿、提炼洞见 | 5 条规则压成可传播、可操作的判断 | 读完后你能直接讲出来，不卡壳 |
| `知识卡片/` | 知识卡片、生成卡片 | 四铁律 + L1/L2/L3，锻造可复用卡片 | 红灯不能为空 |
| `口播/` | 口播、生成口播、创建口播文案 | 8 大叙事库 + 7 步骨架，出可直接念的逐字稿 | 有具体故事/数据，核心观点一句话说清 |

**串联调用**：给我一段口述，直接跑完「灵感 → 卡片 → 口播」，结果按命名约定存进对应目录。

**命名约定（Agent 必须遵守）**

| 类型 | 位置 | 文件名 |
|---|---|---|
| 灵感 | `00-灵感库/` | `YYYY-MM-DD 一句话标题.md` |
| 原子卡 | `07-原子库/` | `KC-领域缩写-序号 卡片名称.md`（如 `KC-投资-04 拿不住是因为没想清楚`） |
| Wiki 长文 | `06-Wiki/` | `标题.md`（成篇知识，不带日期） |
| 内容输出 | `01-Projects/` | `YYYY-MM-DD 标题.md` |

每篇 frontmatter 必填 `type`（`idea` / `knowledge_card` / `oral_script` / `article`）。灵感笔记带 `status`：`captured` → `developed` 或删除。

---

## 四、四条铁律

1. **进库有门槛** — 不认同的、讲不清的、让你困惑的，才配进灵感库；两周没处理的，直接删。
2. **输出前过四问** — 能说出反例吗？能解释两件不相干的事吗？聪明人会质疑哪？比以前的版本新在哪？过不了就删。
3. **每篇接上篇** — 写的时候带一句"上期我说 X，这次发现……"。读者看着你长，你也知道自己在长。
4. **用完必复盘** — 任何知识用完之后，必须过一遍：
   > "我是这么用的，结果是这样的，下次怎么改？"

   **复盘产物按性质归位**（★ 复盘是动作，不是一个目录）：

   | 复盘产出了什么 | 去哪 |
   |---|---|
   | 一条可复用的判断 / 教训 | `07-原子库/` |
   | 一个系统性认识 | `06-Wiki/` |
   | 一个新冒出来的问题 | `00-灵感库/` |

   **复盘不落空 —— 没有产物的复盘不算复盘。**

---

## 五、反模式（明确禁止）

- ❌ **收藏癖**：把"看起来有用"的东西塞进灵感库 —— 那是收藏夹，不是灵感库
- ❌ **跳过验证**：没过门禁四问就输出
- ❌ **AI 原文直接入库**：不经挖矿加工就保存 AI 生成内容
- ❌ **空烧**：灵感库堆着不动，产出不落盘
- ❌ **断环**：第三环的产出不回灌第一环；用完不复盘
- ❌ **无触发条件的原子卡**：只说"未来可能有用"却不写什么情况下用 —— 那是收藏，不是知识
- ❌ **禁词**：赋能 / 打造 / 抓手 / 闭环 / 底层逻辑 / 维度 / 拉齐 / 沉淀 / 生态
- ❌ **并列堆砌**：只有 A、B、C 并列，没有因果 / 转折 / 递进
- ❌ **追求全面**：试图一次讲完所有相关概念
- ❌ **在别处重复本文件的规则**（单一事实源，用链接代替）

---

## 六、Agent 硬性指令

1. **读规则再动手**：处理灵感库前读 `00-灵感库/index.md`；走加工管线前读 `00-灵感库/创作流程.md`；**给建议前先读根目录 `User.md`** —— 不知道用户是谁，建议就是空话
2. **不许替用户思考**：追问，不脑补。用户没确认的表述不算数
3. **不当好好先生**：卡片 / 文案有问题直说，禁止"很好，但是"→ 改为"问题是"
4. **不默认升级**：知识卡片维持当前等级是默认态度，除非用户举出具体经历
5. **不制造伪量化**：禁止百分制、评分、匹配度 —— 改用具体描述

---

## 七、维护规则

- 目录结构变化 / 流程调整 / 新增 Skill → 更新本文件，并更新 frontmatter 的版本号
- **先改现实，再改文件**：新流程跑够 2 周有效，才写进本文件
- **单一事实源**：本文件是唯一权威，不要在其他笔记重复描述规则

---

> **每月问自己**：这个月我从这条流水线上取出了什么？答不上来 = 系统在空转。
